# Upstream pins the MCP SDK to preview builds served from pkg.pr.new, and npm 12
# refuses URL dependencies by default (EALLOWREMOTE). Grant that here so a fresh
# clone can run `npm ci` without command-line flags. Nothing else is relaxed:
# install-time lifecycle scripts stay blocked and git sources stay disabled.
allow-remote=all
